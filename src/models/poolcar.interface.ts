import {DateTime} from "ionic-angular";

export interface PoolCar {
  carName: string;
  name: string;
  contact: number;
  UserId: string;
  to:string;
  from:string;
  date: Date;
  time: string;
  seats: number;
  _id: string;
}
