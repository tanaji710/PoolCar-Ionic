export interface AddpoolcarInterface {
  addharNo: number;
  name: string;
  contact: string;
  poolId: string;
  UserId: string;
  accepted: boolean;
  rejected: boolean;
  pickupPoint: string;
_id: string;
}
